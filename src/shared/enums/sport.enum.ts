/**
 * Sport slugs aligned with SofaScore API path segments (`sport/{slug}/...`).
 * Not exhaustive — the provider may add sports before this enum is updated.
 */
export enum Sport {
  FOOTBALL = 'football',
  BASKETBALL = 'basketball',
  TENNIS = 'tennis',
  CRICKET = 'cricket',
  ICE_HOCKEY = 'ice-hockey',
  AMERICAN_FOOTBALL = 'american-football',
  TABLE_TENNIS = 'table-tennis',
  BASEBALL = 'baseball',
}
